services:
  - type: web
    name: trading-platform
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8000
      - key: HF_TOKEN
        sync: false  # You'll set this in dashboard